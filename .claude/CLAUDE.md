<!--
Project-scoped instructions for working ON this repository.

The root CLAUDE.md is NOT this: it is the recommended global CLAUDE.md that users
copy to ~/.claude/CLAUDE.md (see README, "Recommended CLAUDE.md"). It ships to other
machines, so nothing repo-specific belongs in it — a relative @import placed there
would resolve against the user's ~/.claude/ directory and silently load nothing.

Claude Code loads ./CLAUDE.md and ./.claude/CLAUDE.md, so both apply here. Repo-specific
wiring goes in this file; the distributable template stays portable.
-->

@../AGENTS.md
